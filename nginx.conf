worker_processes auto;
events { worker_connections 1024; }

http {
  # Zona de rate limit por IP: 10 requisições/segundo, até 20 em burst (ver demo no README)
  limit_req_zone $binary_remote_addr zone=api_zone:10m rate=10r/s;
  limit_req_status 429;

  upstream ingestor_backend {
    server ingestor:8080;
  }

  server {
    listen 80;
    server_name _;

    # health check sem rate limit
    location = /health {
      proxy_pass http://ingestor_backend/health;
    }

    location /api/ {
      limit_req zone=api_zone burst=20 nodelay;
      # Sem barra final no proxy_pass: repassa a URI completa (com /api/),
      # que é o prefixo das rotas REST do ingestor-service.
      proxy_pass http://ingestor_backend;
      proxy_set_header Host $host;
      proxy_set_header X-Real-IP $remote_addr;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_read_timeout 10s;
    }
  }
}
